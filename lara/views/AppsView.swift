import SwiftUI

struct AppsView: View {
    var body: some View {
        List {
            
        }
        .navigationTitle("Sideloaded Apps")
        .toolbar {
            ToolbarItem(placement: .navigationBarTrailing) {
                Button("Reload") {
                    
                }
            }
        }
    }
}
